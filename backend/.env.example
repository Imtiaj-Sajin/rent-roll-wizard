# MySQL — same database as ODIN EMS
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=odin_ems

# JWT — must match the JWT_SECRET in the ODIN EMS backend
JWT_SECRET=your-super-secret-jwt-key-change-this
JWT_EXPIRES_IN=24h
